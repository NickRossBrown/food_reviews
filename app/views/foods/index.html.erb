<% content_for(:title, "Food-Index") %>
<h1>Foods</h1>

<div class="card">
    <h1><strong>Featured Foods</strong></h1>
    <hr>

    <% @foods.featured_foods.each do |food| %>
        <h3 class="btn-info"><%= link_to food.name, food_path(food) %></h3>
        <p><strong>From: <%= food.location %></strong></p>
        <p><strong>Price $<%= food.price %></strong></p>
        <% if food.image.attached? %>
            <p><strong>Image:</strong><br><%= image_tag food.image, class: "picture"  %></p>
        <% end %>
    <% end %>
    <h1><strong>Three Most Recent Food Items</strong></h1>
    <hr>
    <% @foods.three_most_recent.each do |food| %>
        <h3 class="btn-info"><%= link_to food.name, food_path(food) %></h3>
        <p><strong>From: <%= food.location %></strong></p>
        <p><strong>Price $<%= food.price %></strong></p>
        <% if food.image.attached? %>
            <p><strong>Image:</strong><br><%= image_tag food.image, class: "picture"  %></p>
        <% end %>
    <% end %>
    <h1><strong>Made in The United States</strong></h1>
    <hr>
    <% @foods.made_in_USA.each do |food| %>
        <h3 class="btn-info"><%= link_to food.name, food_path(food) %></h3>
        <p><strong>From: <%= food.location %></strong></p>
        <p><strong>Price $<%= food.price %></strong></p>
        <% if food.image.attached? %>
            <p><strong>Image:</strong><br><%= image_tag food.image, class: "picture"  %></p>
        <% end %>
    <% end %>

    <h1><strong>The Foods with the Most Reviews</strong></h1>
    <hr>
    <% @foods.most_reviews.each do |food| %>
        <h3 class="btn-info"><%= link_to food.name, food_path(food) %></h3>
        <p><strong>From: <%= food.location %></strong></p>
        <p><strong>Price $<%= food.price %></strong></p>
        <% if food.image.attached? %>
            <p><strong>Image:</strong><br><%= image_tag food.image, class: "picture"  %></p>
        <% end %>
    <% end %>
    <hr>

    <h1><strong>All the Foods Items</strong></h1>
    <% @foods.each do |food| %>
    <h3 class="btn-info"><%= link_to food.name, food_path(food) %></h3>
    <% if food.image.attached? %>
        <p><strong>Image:</strong><br><%= image_tag food.image, class: "picture"  %></p>
    <%end%>
    <% if food.location %>
        <p><strong>From: <%= food.location %></strong></p>
    <%end%>
    <% if food.price %>
        <p><strong>Price $<%= food.price %></strong></p>
    <%end%>
  
    <% if food.user == current_user %>
        <h5><%= link_to 'Edit', edit_food_path(food) %></h5>
        <h5><%= link_to 'Destroy', food, method: :delete, data: { confirm: 'Are you sure?' } %></h5>
        <% end %>
        <hr>
    <% end %>

</div>

<hr>
